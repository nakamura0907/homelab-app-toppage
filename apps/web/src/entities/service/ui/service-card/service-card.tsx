import React from "react";
import { ServiceModel } from "../../types";

type Props = {
    model: ServiceModel
};

export const ServiceCard: React.FC<Props> = () => {
    return (
        <div></div>
    )
}
