import React from "react";
import { ServiceModel } from "../../types"

type Props = {
    models: ServiceModel[];
}

export const ServiceCardList: React.FC<Props> = () => {
    return (
        <div></div>
    )
}
